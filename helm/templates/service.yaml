apiVersion: v1
kind: Service
metadata:
  name: {{ include "clawhermes-ai-go.fullname" . }}
  labels:
    {{- include "clawhermes-ai-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clawhermes-ai-go.selectorLabels" . | nindent 4 }}