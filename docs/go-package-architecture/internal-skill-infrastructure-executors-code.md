# internal/skill/infrastructure/executors/code（已移除包）

当前仓库不存在该目录。旧 Python/goja 代码 Skill executor 已随 Skill instruction capability 重构移除，当前 Skill revision 不包含 code implementation 字段，也没有直接代码执行路由。

当前领域契约见 [`internal/skill/domain`](internal-skill-domain.md)，当前 HTTP surface 见 `api/http/router.go` 的 `registerSkills`。

本页保留原路径索引，避免旧链接失效；它不计入当前 Go package 总数。
