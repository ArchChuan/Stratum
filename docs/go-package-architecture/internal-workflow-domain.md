# internal/workflow/domain

定义 Workflow definition、不可变 version、run、node attempt、event、approval 和 effect intent。

完整导入路径：`github.com/byteBuilderX/stratum/internal/workflow/domain`

该包承载 DAG 与运行状态不变量，不执行 SQL 或外部调用。
